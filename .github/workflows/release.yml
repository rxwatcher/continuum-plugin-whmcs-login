name: Release

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: RXWatcher/silo-plugin-repository/.github/workflows/build-plugin.yml@main
    secrets:
      CATALOG_SSH_KEY: ${{ secrets.CATALOG_SSH_KEY }}
    with:
      plugin_name: silo-plugin-whmcs-login
