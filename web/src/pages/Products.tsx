// Phase 12 fills in the real shuttle-based product selector. This is a
// placeholder so the SPA builds against the routing scaffold in Phase 11.
export default function Products() {
  return (
    <div className="text-muted-foreground">Products page (Phase 12).</div>
  );
}
